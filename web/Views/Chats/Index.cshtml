@model IEnumerable<web.Models.Chat>

@{
    ViewData["Title"] = "Index";
}

<div class="sidemenu">   
    @foreach (var chat in Model)
    {
        <a href= "#" class = "room-button"> @chat.Name </a>
                
    }
        <a href = "#" class="room-button" id="create-room">+</a>
</div>
    <div class = "modal" id = "create-room-modal">
        <div class = "modal-body">
            <form asp-action="CreateRoom" asp-controller="Chats" class ="">
                            
            <button class ="close_modal" type="button" onclick="closeModal()">X</button> 
            <header>Create Room</header>
            <div>
                <input name = "name">
            </div>
            <footer><button>Create</button></footer>


        </form>
    </div>
                    
</div>

<script src ="~/js/site.js"></script>